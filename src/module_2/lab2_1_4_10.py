import pydoc
for x in (0., 1.0, -1.0):
    y = (3*x**3) - (2*x**2)+(3*x)-1
    print("y =", y)

# Python is dinamically typed
# You can use compound operators like in C (+=. -=, ect.)

""" 
Multi line comments also
possible in Python as 
docstrings
"""
